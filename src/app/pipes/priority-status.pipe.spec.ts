import { PriorityPipePipe } from './priority-status.pipe';

describe('PriorityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
